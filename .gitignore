node_modules
dist
dev-dist
*.local
.DS_Store
.env
.vscode

# TS composite build artifacts (emitted by `tsc -b`)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Planning docs — local only, never commit
_local/
CLAUDE.md

# Playwright MCP scratch output
.playwright-mcp/

# Capacitor / Android native build artifacts (the native project IS committed;
# only generated/local bits are ignored). android/ has its own .gitignore too.
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/capacitor-cordova-android-plugins/
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/capacitor.plugins.json
