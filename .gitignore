node_modules
dist
dev-dist
*.local
.DS_Store
.env
.vscode

# TS composite build artifacts (emitted by `tsc -b`)
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Planning docs — local only, never commit
_local/
CLAUDE.md
